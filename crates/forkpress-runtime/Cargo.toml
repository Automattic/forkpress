[package]
name = "forkpress-runtime"
version = "0.1.46"
edition = "2024"

[features]
default = []

[dependencies]
anyhow = "1.0"
flate2 = "1.1"
forkpress-core = { path = "../forkpress-core" }
tar = "0.4"
zip = { version = "2.4", default-features = false, features = ["deflate"] }
